Pure Data external for playing ASDF scenes
==========================================

Don't forget to check out the `pd-lib-builder` submodule with:

    git submodule update --init

The Pd external can be created by simply calling:

    make
